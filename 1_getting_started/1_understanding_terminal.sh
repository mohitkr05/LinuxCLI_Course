# Let us understand the terminal and shell
echo $SHELL

# let us find out the process of the shell 
#ps -ef | grep -i <shellname>

# Identify the parent process 
# The parent process is of terminal which invokes shell 
